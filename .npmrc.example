# For local publish to GitLab Package Registry
# Replace <group> and <project> if needed.

@truflect:registry=https://gitlab.com/api/v4/projects/<project_id>/packages/npm/
//gitlab.com/api/v4/projects/<project_id>/packages/npm/:_authToken=<your_token>
